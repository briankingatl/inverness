{
  "name": "inverness-site",
  "version": "1.0.0",
  "private": true,
  "description": "Inverness HOA static site. Admin panel lives in /admin (local-only, excluded from dist/).",
  "scripts": {
    "build": "node scripts/build-site.js"
  }
}
