# Dependencies (needed locally by scripts/optimize-images.js)
node_modules/

# Local-only admin panel: tracked in git for versioning, but EXCLUDED
# from the dist/ build (see scripts/build-site.js) so it never publishes.

# Build output (this is what gets deployed)
dist/

# Logs
*.log

# OS files
.DS_Store
Thumbs.db
